// Let's not put a whole lot of stuff in this file for now, just keep it
// simple. Just a few colors and general layout constants if necessary.

export const colors = {
  primary: '#0093d5',
  white: '#ffffff',
  primaryCardBackground: '#EDF6F8',
  secondaryCardBackground: '#FAE9B0',
};

// Not sure if we need this default export.
export default {
  colors,
};
